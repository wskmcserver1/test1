name: Debugging with SSH
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run script
        uses: csexton/debugger-action@master
      - name: Sleep
        run: sleep 1h
